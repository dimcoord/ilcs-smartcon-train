## Create synthetic training data for smart contract project

If not already, create a new virtual environment using venv

Install dependecies first:
`pip install -r requirements.txt`

Run `main.py` file either in docx or pdf folder
